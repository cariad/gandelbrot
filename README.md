# gandelbrot

A Golang experiment.

## Usage

To start the HTTP server on port 8080:

```bash
go run .
```

## API

- `GET /` returns `hello`.
- `GET /render` opens a websocket that doesn't do anything.
